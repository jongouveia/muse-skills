---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className = '', ...rest } = Astro.props;
---
<div class={`rounded-lg border bg-card text-card-foreground shadow-sm ${className}`} {...rest}><slot /></div>
